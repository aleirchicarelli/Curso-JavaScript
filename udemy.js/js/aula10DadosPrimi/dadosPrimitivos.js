//Tipos de dados primitivos

const nome1 = "Aleir"; // String
const nome2 = "Chicarelli"; // String
const nome3 = `Teixeira`; // String
const num1 = 29; //Number
const num2 = 11.82; //Number ponto flutuante

let nomeAluno; //Undefined -> não aponta pra local nenhum na memória
const sobrenomeAluno = null; // Nulo -> não aponta pra local nenhum na memória
const aprovado = true;  //boolean -> true (lógico)
const reprovado = false; //boolean -> false (lógico)

console.log( nome1, nome2, nome3, num1, num2); //(typeof) Ver o tipo da variável
